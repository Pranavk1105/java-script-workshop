//Create a function that takes a number as a
//argument and returns negative of that number.
// return negative number without any change

function makeNegative(n) {
  
  return n < 0 ? n : -n;
  console.log(makeNegative(0));
  ;
}


 